---
description: Run a non-technical change review now (plain-language Change Cards in the browser).
---

Invoke the `shipgate` skill to review the current cycle's changes on demand.
Arguments (optional): a baseline ref to diff from. If omitted, use the skill's
baseline resolution.
