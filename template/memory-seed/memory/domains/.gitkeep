# Domain maps live here (one file per domain, e.g. db.md, api.md, ui.md).
# Created on demand by kaizen-sensei as the repo's domains become clear.
# Each map is capped at 200 lines. A fresh repo starts with none.
