# kaizen telemetry — one JSON object per run (A7 schema). Appended by kaizen-sensei only (EVERY run, full or light retro).
# schema: {ts, run_id, workflow, slug, retro_mode:"full"|"light", phase_tokens:{load,prepare,execute,retro}, phase_ms:{load,prepare,execute,retro}, waves, tasks, blocked, confirmed_findings, assumptions_count, self_edits, learnings_seen, metrics:{commits,files_changed,net_loc,tests_touched}}
